/**
 * Barrel — NG-003
 * Re-export server/client env boundaries for convenient imports.
 * Prefer direct imports (server vs client) to make boundaries explicit.
 */
export * from "./server";
export * from "./client";
